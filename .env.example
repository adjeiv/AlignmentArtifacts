# Copy to .env for `docker compose up` (compose loads .env automatically).
# Required - rfc/agents.py calls the real Anthropic API when the frontend
# asks for a task's canary instances (see CLAUDE.md / docker-compose.yml).
ANTHROPIC_API_KEY=
