# Output directory

Place the final deliverable here as:

```
summary_table.md
```

This directory is intentionally empty otherwise — nothing here is a
template to fill in or a starting point to edit. Build `summary_table.md`
from scratch from a live API response, following the structure described in
`../TASK.md` and the layout shown in `../reference/example_format.md`.
